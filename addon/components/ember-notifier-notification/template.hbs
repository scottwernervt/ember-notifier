{{#if hasBlock}}
  {{yield}}
{{else}}
  {{component
    iconComponent
    icon=notification.icon}}
  {{component
    contentComponent
    title=notification.title
    message=notification.message
    contentComponent=notification.contentComponent
    setOption=(action "setOption")}}
  {{component
    closeComponent
    close=close}}
{{/if}}
